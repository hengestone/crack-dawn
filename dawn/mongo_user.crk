import crack.db.mongo_support OID;
import dawn.user User;

class MongoUser : User {
  oper init() : User() { }
  OID getOID() {
    return _id;
  }

  void setOID(OID newOID) {
    _id = newOID;
  }
}
